{
  "name": "product-sale-website-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-backend": "npm install --prefix backend",
    "install-frontend": "npm install --prefix frontendFinal",
    "build-frontend": "npm run build --prefix frontendFinal",
    "build": "npm run install-backend && npm run install-frontend && npm run build-frontend",
    "start": "npm start --prefix backend"
  }
}
